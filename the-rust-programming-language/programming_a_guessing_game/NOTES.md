# Notes
## Shadowing
Shadowing lets us reuse the already created guess variable name rather than forcing us to create two unique variables, such as guess_str and guess, for example.

## Arms
A match expression is made up of arms. An arm consists of a pattern to match against, and the code that should be run if the value given to match fits that arm’s pattern. Rust takes the value given to match and looks through each arm’s pattern in turn.

## Commands
```
// Add a package
cargo add <package_name>

// Update a package
cargo update <package_name>

// Open the cargo documentation
cargo ddoc --open
```